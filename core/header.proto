syntax = "proto3";
package make87_messages.core;

import "google/protobuf/timestamp.proto";

message Header {
    google.protobuf.Timestamp timestamp = 1;
    uint64 reference_id = 2;
    // Slash-based entity path, e.g., "cameras/1/intrinsic"
    string entity_path = 3;
}
